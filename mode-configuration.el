;; File to enable modes

; Enable helm mode (Smart completion for find-file, M-x ... etc)
(helm-mode)

; Enable nyan mode (A cat that scrolls at the bottom of the screen)
(nyan-mode)

;; Enable yasnippet
(yas-global-mode 1)
